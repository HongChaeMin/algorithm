#include<stdio.h>
#include<math.h>

int main()
{
	int a, b, c, d, e, n=0, m=0, j=0 , i=0, t=0, x=0;

	scanf("%d %d %d %d %d", &a, &b, &c, &d, &e);

	n=a*a;
	m=b*b;
	j=c*c;
	i=d*d;
	t=e*e;

	x=n+m+j+i+t;

	printf("%d", x%10);

}
